# Doushu v2 当前流程

核心结构：

```text
输入 → 数据准备 → 方法模式推演（河洛/飞星） → 完整命局模式（按需） → 专项/流月报告 → 交付
```

其中“推演”是唯一产生命理判断的层；报告层只负责选择、翻译和组织已经登记的判断。

## 一、输入阶段

用户提供出生日期、时间、性别、出生地和报告需求。

如果出生时间不明确、有争议或存在跨时辰风险：

1. 只询问可确定的时间范围和必要校时资料；
2. 用户明确要求比较后，以候选比较模式准备各候选盘；
3. 排盘完成后再对比差异；
4. 经历吻合只记录为支持线索，不升级为已确认出生时刻。

如果时间已经明确：直接按该时辰分析，不做无必要的双盘。

## 二、数据准备阶段

按 `chart_key` 生成或复用：

- 结构化命盘；
- 庙旺数据；
- 当前大限和流年数据；
- 案例检索结果；
- 前事验证记录。

按 `references/data-cache-contract-v1.md` 检查分层缓存键、哈希及覆盖范围，有效时复用，仅重建失效数据。

涉及流年、流月或跨领域联动时，数据准备阶段必须运行 `scripts/validate-monthly-flow.js`。校验结果写入 `monthlyDataStatus`：

- `complete`：月干支、流月命宫、十二宫、月干四化和叠宫完整；
- `partial`：只有节气月干支；
- `fallback`：部分字段由本地算法补齐；
- `unavailable`：没有可靠流月数据。

只有 `complete` 或明确标注 `fallback` 时，报告才能输出具体流月命宫、月干四化和叠宫；`partial` 只能使用节气月干支做辅助观察。

## 三、分析方向确认

根据用户需求确认：

- 健康；
- 事业；
- 感情；
- 社交；
- 财务；
- 迁移；
- 家庭；
- 完整命局或多个方向组合。

用户只说“分析一下”时，先询问主要方向；用户已经明确方向时，不加载无关领域。

## 四、方法模式推演阶段

先做当前任务所有方向共用的基础推演，并先锁定 `method_profile`：

1. 资料核验；
2. 始终完成排盘后的飞星基础层：生年四化、命宫飞化、自化/互化/连环忌、大限、流年和流月因果链数据；
3. `helu`：命宫起逆布 1—12 坐标，标出命1、疾6、官9、一六共宗、命财官气数位和主问宫气数位；
4. `helu`：以原命盘宫干完成河洛跟踪；`feixing`：将飞星基础链提升为主断，并将河洛、中州、许铨仁作为分栏对照；
5. 两种模式都检查主问宫、本宫与对宫；三方四正只按当前模式的格局或流月承接需要调用；
6. 当前大限背景；
7. 指定流年触发；
8. 运行流月数据校验器，确认流月命宫、十二宫、月干四化和叠宫状态（涉及流月时）；
9. 只有需要入三合方论格局时，才补充三方四正；
10. 前事验证、结论登记和资料缺口。

如果 `report_mode = complete`，在上述推演后必须进入 `03-complete-report`：分别完成飞星独立链、河洛独立链、关键三方四正核验、全盘现实专题和选择路径对照，再进入质量与交付模块。

方法模式的实际顺序：

```text
helu：1—12坐标 → 命疾福 → 命财官气数位 → 主问宫气数位 → 原盘宫干四化 → 本宫与对宫
feixing：原局电源 → 命宫飞化/自化 → 大限体用 → 流年开关 → 流月宫位线与四化线 → 必要对照
证据合并：所选方法的因果链 ∩ 现实场景 → 触发条件、时间窗口、验证点和行动
```

只有一条证据链时，输出观察点或条件性可能；主轴因果链、限年承接和现实反馈出现重叠时，才再展开具体结论。两种方法同时启用时，分栏对照，不合并成一条伪因果链。

然后根据方向进入专项深入分析：

- 健康：身体状态、生活方式、脏腑观察和医疗边界；
- 事业：工作内容、组织环境、权责、收入、风险和运限；
- 感情：关系需求、互动、冲突、承诺、边界和共同生活；
- 社交：朋友、同事、客户、团队、师长、公共社交和合作；
- 完整命局：按用户要求组合多个方向。

如果任务涉及大限、流年、流月、贵人、合作、迁移或多个领域的联动，追加加载 `references/dynamic-cross-domain-v1.md`。动态模块先判断时间层级和事件活跃等级，再把机会窗口、风险窗口及跨领域传导路径登记到动态结论表；没有可靠流年/流月数据时不得补写具体时间。

这一阶段产出的是当前任务所需的分析事实和判断，不是最终用户报告。

## 五、专项报告阶段

按照用户需求加载一个或多个专项模块：

- 健康：把健康深入分析转换为身体状态和生活管理报告；
- 事业：把事业深入分析转换为工作条件、风险和选择报告；
- 感情：把感情深入分析转换为关系运行和边界报告；
- 社交：把社交深入分析转换为关系筛选、沟通和合作报告；
- 完整报告：按用户指定的领域组合输出，不重复通用依据。

专项模块不重新推演全盘，只把所选方法模式的推演结果应用到当前主题。多个专项模块共享同一结论登记，不互相复制完整依据。

## 六、用户呈现阶段

非感情主题统一使用四个可见结构；感情专项改用其固定模板：

1. 先说结论；
2. 具体对象、变化和触发条件；
3. 时间范围、验证点和替代解释；
4. 与主题匹配的可执行建议。

感情专项必须按“排盘依据 → 先说结论 → 对象外貌/家庭层次/性格/相处 → 相识方向/场合/事件/进入建议 → 公历时间表（括号附农历）→ 关系底层模式 → 方法来源”输出。时间表默认列出 6—10 个重点公历窗口；用户明确要求“很多/一大批时间点”时再扩展到 10 个以上，每行写触发遇见对象的原因；默认只交付 Markdown，不生成 HTML 或 PDF。

核心断语不得只写“有机会、遇贵人、压力增加、财运变好、关系有变化”。每条断语必须能回溯到所选模式的主宫/源宫、气数位或飞星落宫、化曜路径，并写出可观察的现实场景。

完整可能性通过“有稳定关系/没有稳定关系”“有稳定工作/正在换工作”等现实分流呈现，而不是增加大量专业标题。用户正文使用 `references/user-rendering-v3.md`：现实结论写在正文，命理推断压缩到段末括号。

健康内容必须明确到具体脏腑观察方向及可能状态，例如脾胃、肝胆、心与心神、肺与皮肤、肾与水液、筋骨和眼目；不能笼统写成“注意身体”。

用户验证不做问题清单，而是写成一段自然引导，最多保留两个区分度最高的确认点。报告完成后调用 `restrained-professional-voice` 审校。

## 七、重复检查阶段

只检查三类重复：

1. 同一核心判断是否在多个章节完整出现；
2. 同一组专业依据是否被反复罗列；
3. 同一行动建议是否重复。

发现重复时，保留最相关章节的完整解释，其他地方只保留现实表现或一句引用。

## 八、交付阶段

1. 语言审校；
2. 保存执行记录与正文，先运行河洛方法 Preflight，再运行 preflight 生成前检查；
3. 感情专项检查 Markdown 结构、日期核验和链接；其他需要版式的任务再生成 PDF；
4. 用户明确要求 HTML/PDF 时才进行对应渲染检查；
5. 核对关键数据、标题、分页和字体，保存 QA 记录和产物哈希；
6. 更新完成状态，运行 final 验收，通过后输出 Markdown；用户明确要求时再追加 HTML/PDF。

## 九、与旧流程的区别

旧流程：

```text
每次都完整分析全盘 → 分别重写专项报告 → 再次整理用户版 → 检查重复
```

新流程：

```text
输入确认 → 方法模式锁定 → 坐标/飞星因果链 → 按方向深入分析 → 具体性 Preflight → 对应专项报告 → 轻量去重 → Markdown（用户要求时追加 HTML/PDF）
```

核心变化：输入层决定分析范围；方法模式决定推理主轴；深入分析层负责该方向的深度和现实落地；专项报告层负责应用和易读；交付层负责文件质量。
